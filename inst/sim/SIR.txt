deriv(I) <- foi * S - dropout * I
deriv(A) <- dropout * I

initial(S) <- m - I0 - A0
initial(I) <- I0
initial(A) <- A0


output(Inc) <- foi * S
output(FOI) <- foi
output(Re) <- (beta / dropout) * (S / m)
output(PrEx) <- 0

S <- m - I - A

foi <- beta * (I / m)

m <- user(1000)
I0 <- user(5)
A0 <- user(5)

beta <- user(0.3)
dropout <- user(0)
